# Copilot repository instructions

Follow `AGENTS.md` and the active work item's specification and acceptance contract. Governance files are owner-locked. Do not enable live trading, weaken checks, expose secrets, use floating point for money, or allow malformed/unknown state to create orders. Link code and tests to task and AC IDs. Run `python3 scripts/self_check.py` and the full relevant test suite before completion.

